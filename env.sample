# Copy what you need. The web app reads NEXT_PUBLIC_* at build time; the rest are
# server-side (Railway env). Never commit real secrets.

# --- Web app (Next.js on Railway) ---
NEXT_PUBLIC_BACKLOG_ADDRESS=0x37284f74Ce61378522CFC39fDE4FF9d40A195bb8

# --- GitHub OAuth (Connect GitHub → score repos) ---
# GITHUB_CLIENT_ID=Ov23li...
# GITHUB_CLIENT_SECRET=...
# APP_URL=https://your-app.up.railway.app   # canonical origin for the OAuth redirect_uri

# --- Scoring fallback (optional) ---
# GITHUB_MODELS_TOKEN=ghp_...        # shared server token used if a visitor's own token lacks models:read
